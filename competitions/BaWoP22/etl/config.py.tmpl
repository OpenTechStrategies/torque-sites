# e.g. https://torque.leverforchange.org/BaWoP22
wiki_url = "${WIKI_URL}"
username = "${MEDIAWIKI_ADMIN_USERNAME}"
password = "${MEDIAWIKI_ADMIN_PASSWORD}"

# Get from opass at clients/lever-for-change/submittable
submittable_api_key = "${SUBMITTABLE_API_KEY}"
