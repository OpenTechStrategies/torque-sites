---

- hosts: mysql
  become: true
  become_user: "{{ deployment_user }}"
  roles:
    - mysql

- hosts: mediawiki
  become: true
  become_user: "{{ deployment_user }}"
  roles:
    - mediawiki
    - activitylog
    - simplebook
    - embed_video
    - permissions
    - simplefavorites
    - simplesaml
    - torque
    - exporttables
    - simplemaps
    - emulategroup
    - NewIdeas
    - finishmediawiki
